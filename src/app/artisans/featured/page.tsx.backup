import { Suspense } from "react";
import FeaturedArtisansContent from "./FeaturedArtisansContent";

export default function FeaturedArtisansPage() {
  return (
    <Suspense fallback={
      <div className="min-h-screen bg-gradient-to-b from-background to-background/95 flex items-center justify-center">
        <div className="animate-pulse">
          <div className="h-8 w-48 bg-primary/10 rounded mb-8 mx-auto"></div>
          <div className="grid grid-cols-1 md:grid-cols-2 gap-8">
            {[1,2,3,4].map(i => (
              <div key={i} className="h-96 bg-primary/10 rounded-xl"></div>
            ))}
          </div>
        </div>
      </div>
    }>
      <FeaturedArtisansContent />
    </Suspense>
  );
}
