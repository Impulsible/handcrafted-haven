import { Suspense } from "react";
import Image from "next/image";
import Link from "next/link";
import { Users, Award, Heart, Globe, Sparkles, ArrowRight } from "lucide-react";
import { Button } from "@/components/ui/button";

// Client component that uses searchParams (if any)
function AboutContent() {
  // Your existing about page content here
  return (
    <div className="min-h-screen bg-gradient-to-b from-background to-background/95">
      <div className="container mx-auto px-4 sm:px-6 lg:px-8 py-12 max-w-7xl">
        {/* Hero Section */}
        <div className="text-center max-w-3xl mx-auto mb-16">
          <div className="inline-flex items-center px-4 py-2 bg-primary/10 rounded-full mb-6">
            <Sparkles className="w-4 h-4 text-primary mr-2" />
            <span className="text-sm font-semibold text-primary">OUR STORY</span>
          </div>
          <h1 className="text-4xl sm:text-5xl lg:text-6xl font-heading font-bold mb-6">
            About <span className="text-gradient">Handcrafted Haven</span>
          </h1>
          <p className="text-lg sm:text-xl text-muted-foreground">
            Connecting talented artisans with people who appreciate the beauty of handmade crafts.
          </p>
        </div>

        {/* Mission Section */}
        <div className="grid md:grid-cols-2 gap-12 items-center mb-20">
          <div>
            <h2 className="text-3xl font-heading font-bold mb-4">Our Mission</h2>
            <p className="text-muted-foreground mb-6">
              We believe in preserving traditional craftsmanship while supporting 
              artisans in the digital age. Handcrafted Haven was born from a desire 
              to create a space where makers can share their passion and creativity 
              with the world.
            </p>
            <div className="space-y-4">
              {[
                { icon: <Users className="h-5 w-5" />, text: "Support over 200+ active artisans" },
                { icon: <Award className="h-5 w-5" />, text: "Guaranteed authentic handmade items" },
                { icon: <Heart className="h-5 w-5" />, text: "Ethical and sustainable practices" },
                { icon: <Globe className="h-5 w-5" />, text: "Global community of creators" }
              ].map((item, i) => (
                <div key={i} className="flex items-center gap-3">
                  <div className="p-2 bg-primary/10 rounded-lg text-primary">
                    {item.icon}
                  </div>
                  <span className="text-foreground">{item.text}</span>
                </div>
              ))}
            </div>
          </div>
          <div className="relative h-96 rounded-2xl overflow-hidden">
            <div className="absolute inset-0 bg-gradient-to-br from-primary/20 to-secondary/20" />
            <div className="absolute inset-0 flex items-center justify-center">
              <span className="text-6xl">🎨</span>
            </div>
          </div>
        </div>

        {/* Stats Section */}
        <div className="grid grid-cols-2 md:grid-cols-4 gap-6 mb-20">
          {[
            { label: "Active Artisans", value: "200+" },
            { label: "Handmade Items", value: "1,500+" },
            { label: "Happy Customers", value: "10k+" },
            { label: "Countries", value: "30+" }
          ].map((stat, i) => (
            <div key={i} className="text-center p-6 bg-card border border-primary/10 rounded-xl">
              <div className="text-3xl font-bold text-primary mb-2">{stat.value}</div>
              <div className="text-sm text-muted-foreground">{stat.label}</div>
            </div>
          ))}
        </div>

        {/* CTA Section */}
        <div className="bg-gradient-to-r from-primary/10 via-secondary/10 to-accent/10 rounded-2xl p-12 text-center">
          <h2 className="text-3xl font-heading font-bold mb-4">
            Join Our <span className="text-gradient">Community</span>
          </h2>
          <p className="text-muted-foreground mb-8 max-w-2xl mx-auto">
            Whether you're an artisan looking to share your work or a collector 
            seeking unique pieces, we'd love to have you.
          </p>
          <div className="flex flex-col sm:flex-row gap-4 justify-center">
            <Link href="/artisans/featured">
              <Button className="bg-gradient-to-r from-primary to-secondary text-white px-8 py-6 text-lg">
                Meet Our Artisans
                <ArrowRight className="ml-2 h-5 w-5" />
              </Button>
            </Link>
            <Link href="/contact">
              <Button variant="outline" className="border-primary text-primary px-8 py-6 text-lg">
                Contact Us
              </Button>
            </Link>
          </div>
        </div>
      </div>
    </div>
  );
}

export default function AboutPage() {
  return (
    <Suspense fallback={
      <div className="min-h-screen bg-gradient-to-b from-background to-background/95">
        <div className="container mx-auto px-4 sm:px-6 lg:px-8 py-12 max-w-7xl">
          <div className="animate-pulse">
            <div className="h-8 w-48 bg-primary/10 rounded mb-8 mx-auto"></div>
            <div className="h-64 bg-primary/10 rounded-xl mb-8"></div>
            <div className="grid grid-cols-2 md:grid-cols-4 gap-6 mb-8">
              {[1,2,3,4].map(i => (
                <div key={i} className="h-32 bg-primary/10 rounded-xl"></div>
              ))}
            </div>
          </div>
        </div>
      </div>
    }>
      <AboutContent />
    </Suspense>
  );
}