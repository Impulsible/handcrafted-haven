// Force dynamic rendering to avoid prerender issues with useSearchParams
export const dynamic = 'force-dynamic';

import { Suspense } from "react";
import Link from "next/link";
import { Users, Award, Heart, Globe, Sparkles, ArrowRight } from "lucide-react";
import { Button } from "@/components/ui/button";

function AboutContent() {
  return (
    <div className="min-h-screen bg-gradient-to-b from-background to-background/95 overflow-x-hidden">
      <div className="container mx-auto px-4 sm:px-6 lg:px-8 py-8 sm:py-12 max-w-7xl w-full">
        {/* Hero Section */}
        <div className="text-center max-w-3xl mx-auto mb-12 sm:mb-16">
          <div className="inline-flex items-center px-3 sm:px-4 py-1.5 sm:py-2 bg-primary/10 rounded-full mb-4 sm:mb-6">
            <Sparkles className="w-3 h-3 sm:w-4 sm:h-4 text-primary mr-1.5 sm:mr-2" />
            <span className="text-xs sm:text-sm font-semibold text-primary">OUR STORY</span>
          </div>
          <h1 className="text-3xl sm:text-4xl lg:text-5xl xl:text-6xl font-heading font-bold mb-4 sm:mb-6 px-2">
            About <span className="text-gradient">Handcrafted Haven</span>
          </h1>
          <p className="text-base sm:text-lg lg:text-xl text-muted-foreground px-4">
            Connecting talented artisans with people who appreciate the beauty of handmade crafts.
          </p>
        </div>

        {/* Mission Section */}
        <div className="grid md:grid-cols-2 gap-8 lg:gap-12 items-center mb-16 lg:mb-20">
          <div className="px-2 sm:px-0">
            <h2 className="text-2xl sm:text-3xl font-heading font-bold mb-3 sm:mb-4">Our Mission</h2>
            <p className="text-sm sm:text-base text-muted-foreground mb-4 sm:mb-6">
              We believe in preserving traditional craftsmanship while supporting 
              artisans in the digital age. Handcrafted Haven was born from a desire 
              to create a space where makers can share their passion and creativity 
              with the world.
            </p>
            <div className="space-y-3 sm:space-y-4">
              {[
                { icon: <Users className="h-4 w-4 sm:h-5 sm:w-5" />, text: "Support over 200+ active artisans" },
                { icon: <Award className="h-4 w-4 sm:h-5 sm:w-5" />, text: "Guaranteed authentic handmade items" },
                { icon: <Heart className="h-4 w-4 sm:h-5 sm:w-5" />, text: "Ethical and sustainable practices" },
                { icon: <Globe className="h-4 w-4 sm:h-5 sm:w-5" />, text: "Global community of creators" }
              ].map((item, i) => (
                <div key={i} className="flex items-center gap-2 sm:gap-3">
                  <div className="p-1.5 sm:p-2 bg-primary/10 rounded-lg text-primary shrink-0">
                    {item.icon}
                  </div>
                  <span className="text-sm sm:text-base text-foreground break-words">{item.text}</span>
                </div>
              ))}
            </div>
          </div>
          <div className="relative h-64 sm:h-80 lg:h-96 rounded-2xl overflow-hidden mx-2 sm:mx-0">
            <div className="absolute inset-0 bg-gradient-to-br from-primary/20 to-secondary/20" />
            <div className="absolute inset-0 flex items-center justify-center">
              <span className="text-4xl sm:text-5xl lg:text-6xl">🎨</span>
            </div>
          </div>
        </div>

        {/* Stats Section */}
        <div className="grid grid-cols-2 md:grid-cols-4 gap-3 sm:gap-4 lg:gap-6 mb-16 lg:mb-20">
          {[
            { label: "Active Artisans", value: "200+" },
            { label: "Handmade Items", value: "1,500+" },
            { label: "Happy Customers", value: "10k+" },
            { label: "Countries", value: "30+" }
          ].map((stat, i) => (
            <div key={i} className="text-center p-3 sm:p-4 lg:p-6 bg-card border border-primary/10 rounded-xl">
              <div className="text-xl sm:text-2xl lg:text-3xl font-bold text-primary mb-1 sm:mb-2 break-words">{stat.value}</div>
              <div className="text-xs sm:text-sm text-muted-foreground break-words">{stat.label}</div>
            </div>
          ))}
        </div>

        {/* CTA Section */}
        <div className="bg-gradient-to-r from-primary/10 via-secondary/10 to-accent/10 rounded-xl sm:rounded-2xl p-6 sm:p-8 lg:p-12 text-center mx-2 sm:mx-0">
          <h2 className="text-2xl sm:text-3xl lg:text-3xl font-heading font-bold mb-3 sm:mb-4 px-2">
            Join Our <span className="text-gradient">Community</span>
          </h2>
          <p className="text-sm sm:text-base text-muted-foreground mb-6 sm:mb-8 max-w-2xl mx-auto px-4">
            Whether you&apos;re an artisan looking to share your work or a collector 
            seeking unique pieces, we&apos;d love to have you.
          </p>
          <div className="flex flex-col sm:flex-row gap-3 sm:gap-4 justify-center items-stretch sm:items-center px-4 sm:px-0">
            <Link href="/artisans/featured" className="w-full sm:w-auto">
              <Button className="bg-gradient-to-r from-primary to-secondary text-white px-6 sm:px-8 py-4 sm:py-6 text-sm sm:text-lg w-full whitespace-normal h-auto min-h-[3rem]">
                <span className="break-words">Meet Our Artisans</span>
                <ArrowRight className="ml-2 h-4 w-4 sm:h-5 sm:w-5 shrink-0" />
              </Button>
            </Link>
            <Link href="/contact" className="w-full sm:w-auto">
              <Button variant="outline" className="border-primary text-primary px-6 sm:px-8 py-4 sm:py-6 text-sm sm:text-lg w-full whitespace-normal h-auto min-h-[3rem]">
                <span className="break-words">Contact Us</span>
              </Button>
            </Link>
          </div>
        </div>
      </div>
    </div>
  );
}

export default function AboutPage() {
  return (
    <Suspense fallback={
      <div className="min-h-screen bg-gradient-to-b from-background to-background/95 overflow-x-hidden">
        <div className="container mx-auto px-4 sm:px-6 lg:px-8 py-8 sm:py-12 max-w-7xl w-full">
          <div className="animate-pulse">
            <div className="h-6 sm:h-8 w-32 sm:w-48 bg-primary/10 rounded mb-6 sm:mb-8 mx-auto"></div>
            <div className="h-48 sm:h-64 bg-primary/10 rounded-xl mb-6 sm:mb-8"></div>
            <div className="grid grid-cols-2 md:grid-cols-4 gap-3 sm:gap-4 lg:gap-6 mb-6 sm:mb-8">
              {[1,2,3,4].map(i => (
                <div key={i} className="h-24 sm:h-32 bg-primary/10 rounded-xl"></div>
              ))}
            </div>
          </div>
        </div>
      </div>
    }>
      <AboutContent />
    </Suspense>
  );
}